import pandas as pd
import numpy as np

def init_app():
    print('Initializing the things...')


# function to predict the flower using the model
def predict(query_data):

    # X = list(query_data.dict().values())
    # print('x:',X)
    # X=pd.DataFrame([X], columns=['Status of existing checking account','Purpose','Credit amount','Duration in month',
    #                              'Present employment since','Credit history','Savings account/bonds','Property',
    #                              'Age in years'])
    # print('info:',X.info())
    
    return None

# function to retrain the model as part of the feedback loop


